# Tests

The validation workflow checks YAML syntax, identifier formats, and duplicate identifiers across the repository. Future releases should add cross-file referential-integrity checks and JSON Schema validation.
