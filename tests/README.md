# Tests

The validation workflow currently checks YAML syntax, identifier formats, and duplicate identifiers.

Future releases should add cross-file referential-integrity checks and JSON Schema validation for canonical entities.
